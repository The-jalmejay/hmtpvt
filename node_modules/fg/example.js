'use strict';

var fg = require('./index.js');

fg('postgres://postgres@localhost/test', function (client) {

  return client.query(client.select().from('things').where({'name': 'baz'}))
  .then(function (result) {
    console.log(result);
  });

});
